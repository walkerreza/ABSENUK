import 'package:get/get.dart';

class ToolsController extends GetxController {
  // Logika untuk menu tools bisa ditambahkan di sini jika diperlukan di masa depan.
  // Untuk saat ini, controller ini bisa kosong.
}
